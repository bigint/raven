# Database
DATABASE_URL=postgresql://raven:raven@localhost:5432/raven

# Redis
REDIS_URL=redis://localhost:6379

# Auth
BETTER_AUTH_SECRET=changeme
BETTER_AUTH_URL=http://localhost:4000
APP_URL=http://localhost:3000

# App
NEXT_PUBLIC_API_URL=http://localhost:4000
API_PORT=4000

# Encryption
ENCRYPTION_SECRET=changeme-32-byte-hex

# bigRAG is configured at runtime via Admin → Settings → Knowledge.
# Enable RAG, set the base URL, and paste an API key minted from the bigRAG Studio.
