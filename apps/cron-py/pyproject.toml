[project]
name = "raven-cron"
version = "0.1.0"
description = "Raven AI Gateway - Cron Worker"
requires-python = ">=3.13"
dependencies = [
    "apscheduler>=3.10.4",
    "psycopg2-binary>=2.9.9",
    "pydantic-settings>=2.7.0",
]

[tool.ruff]
target-version = "py313"
line-length = 100
